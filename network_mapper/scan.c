#include "../ft_nmap.h"

bool perform_scan(t_input *input, t_ipaddr *ipaddr, int scan)
{
    if (scan == SYN_SCAN || scan == ALL_SCAN)
    {
        // Ma3ert's
    }
    else if (scan == NULL_SCAN || scan == ALL_SCAN)
    {
        // Ma3ert's
    }
    else if (scan == FIN_SCAN || scan == ALL_SCAN)
    {
        // Ma3ert's
    }
    else if (scan == XMAS_SCAN || scan == ALL_SCAN)
    {
        // Rigor's
    }
    else if (scan == ACK_SCAN || scan == ALL_SCAN)
    {
        // Rigor's
    }
    else if (scan == UDP_SCAN || scan == ALL_SCAN)
    {
        // Rigor's
    }
    return true;
}
